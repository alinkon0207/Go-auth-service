tmp/
.air.toml

start.sh

# macOS
.DS_Store

# vim
.*.sw*

# binary
/auth-service

# generated
# docs are generated by `swag init`. They are being committed to repo for now to make sure project can be compiled. We may change this in future.
# /docs

# database
dbData/